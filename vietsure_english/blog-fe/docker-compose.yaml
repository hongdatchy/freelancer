services:
  nextjs-app:
    container_name: nextjs-app-prod
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - vietsure-network

networks:
  vietsure-network:
    name: vietsure-network
    external: true
